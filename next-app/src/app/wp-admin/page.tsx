import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Wp Admin',
  description: 'Wp Admin page - Access and manage your content',
};

export default function Page() {
  return (
    <div>
      {/* Placeholder Page */}
    </div>
  );
}