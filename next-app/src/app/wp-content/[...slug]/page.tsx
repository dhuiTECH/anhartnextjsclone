import { Metadata } from 'next';
import Navigate to=" from '@/components/Navigate to="';

export const metadata: Metadata = {
  title: 'Wp Content',
  description: 'Wp Content page - Access and manage your content',
};

export default function Navigate to="Page() {
  return <Navigate to=" />;
}