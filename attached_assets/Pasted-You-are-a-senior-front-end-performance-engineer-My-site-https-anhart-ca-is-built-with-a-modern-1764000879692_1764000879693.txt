You are a senior front-end performance engineer.

My site https://anhart.ca is built with a modern JS framework (Vite/React/Vue/Svelte) and currently scores ~60-85 on mobile PageSpeed because of render-blocking chunked CSS/JS, legacy polyfills, and a heavy Google Tag Manager.

Do ALL of the following automatically without breaking anything:

1. Make the main CSS non-render-blocking:
   - Find the main CSS chunk (currently something like ...chunks/509360d7f779677.css)
   - Change its <link> tag to:
     <link rel="stylesheet" href="that-file.css" media="print" onload="this.media='all'">
   - Add <noscript> fallback for it

2. Defer every single JavaScript chunk:
   - Add defer to every <script type="module"> or <script src="...chunks/...js">
   - If any script is type="module" without defer, add defer

3. Eliminate legacy polyfills (the 14 KB warning):
   - In vite.config.js / rollup options / webpack config, set target: "es2020" or higher
   - Or add to vite.config.js:
     build: { target: 'es2020' }

4. Lazy-load Google Tag Manager only after user interaction:
   - Replace the current GTM script with this noscript + lazy version:
     <!-- GTM lazy -->
     <noscript><iframe src="https://www.googletagmanager.com/ns.html?id=YOUR-ID" height="0" width="0" style="display:none;visibility:hidden"></iframe></noscript>
     <script>
       document.addEventListener('scroll',loadGTM, {once:true});
       document.addEventListener('mousemove',loadGTM, {once:true});
       document.addEventListener('touchstart',loadGTM, {once:true});
       function loadGTM() {
         const s=document.createElement('script');
         s.src='https://www.googletagmanager.com/gtag/js?id=YOUR-ID';
         s.async=true; document.head.appendChild(s);
         window.dataLayer=window.dataLayer||[];
         function gtag(){dataLayer.push(arguments);}
         gtag('js', new Date());
         gtag('config', 'YOUR-ID');
       }
     </script>
   - Replace YOUR-ID with the actual G-XXXXXX ID

5. Preload your hero image (or hero video poster) in <head>

6. Add proper cache headers in .htaccess for all .js and .css chunks (1 year)

Do everything automatically. After finishing, run a mobile PageSpeed test on https://anhart.ca and report the new score.

Zero visible changes for users — only performance fixes.